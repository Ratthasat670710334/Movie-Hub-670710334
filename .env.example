# คัดลอกไฟล์นี้เป็น .env แล้วใส่ API Key (v3) ของตัวเองหลังเครื่องหมาย = โดยไม่ต้องมีเครื่องหมายคำพูด
# ตัวแปรต้องขึ้นต้นด้วย REACT_APP_ เท่านั้น CRA ถึงจะส่งให้โค้ดฝั่งเบราว์เซอร์
# แก้ไฟล์นี้แล้วต้องหยุดและรัน npm start ใหม่ (ไฟล์ .env จะไม่ถูก commit)
REACT_APP_TMDB_KEY=f1111ed3f1d5dab1541b868bd161e0e3
